# Variables d'environnement pour le projet SahelVert
# Remplacez les valeurs par vos valeurs réelles avant déploiement

SECRET_KEY="django-insecure-1v*2n(r06on)c8h=j(t*_b%!toou3b@+r28s6ccq*x=xy(o1d5"
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost,0.0.0.0

# Database (SQLite par défaut). Pour Postgres, remplissez DB_ENGINE, DB_NAME, DB_USER, DB_PASSWORD, DB_HOST, DB_PORT
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Static & Media
STATIC_URL=/static/
STATIC_ROOT=staticfiles
MEDIA_URL=/media/
MEDIA_ROOT=media

# Email (SMTP) - mettez à jour avec vos informations réelles
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=webmaster@localhost

# Contact administrateur
ADMIN_EMAIL=admin@example.com

# Clés externes / services (placeholders)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

